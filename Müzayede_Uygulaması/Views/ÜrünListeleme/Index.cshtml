@model List<Müzayede_Uygulaması.Models.ÜrünListeleme>

@{
    ViewData["Title"] = "Index";
}
 <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://getbootstrap.com/docs/5.3/assets/css/docs.css" rel="stylesheet">
    <title>Bootstrap Example</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>


<h1>Ürünler Listesi</h1>
<br />
@foreach (var item in Model)
{
   
    <div class="card mb-3" style="max-width: 650px; max-height:400px;">
        <div class="row g-0">
            <div class="col-md-4">
                <img src="@item.ResimYolu" class="bd-placeholder-img img-fluid rounded-start" width="100%" height="280" />
            </div>
            <div class="col-md-8">
                <div class="card-body" >
                    <h5 class="card-title">@item.Başlık</h5>
                    <p class="card-text">@item.Açıklama</p>
                    <p class="card-text"><small class="text-body-secondary"><a asp-action="Details" asp-route-id="@item.Id">Detaylar</a></small></p>
                    <div>Teklif: @item.Fiyat ₺</div>
                    <div>İlanı veren: @item.User.Email</div>
                </div>
            </div>
        </div>
    </div>
 
}
@if (!Model.Any())
{
    <p>Aradığınız ürün bulunamadı?</p>
}    
@* <p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Başlık)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Açıklama)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fiyat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ResimYolu)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Satıldımı)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Başlık)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Açıklama)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fiyat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ResimYolu)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Satıldımı)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table> *@
